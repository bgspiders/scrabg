# 文章数据库类型选择（用于后端数据管理和爆虫数据存储）
# 支持值: mysql, mongodb
# 默认: mysql
# 注意: success_worker.py 优先级为 MongoDB > MySQL > Redis
ARTICLE_DB_TYPE=mysql

# Redis 配置
# 格式: redis://[:password@]host:port/db
# 无密码: redis://localhost:6379/0
# 有密码: redis://:your_redis_password@localhost:6379/0
# 或者: redis://username:password@localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# MySQL 配置（若不使用可留空）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=your_username
# MySQL 密码配置说明：
# 1. 如果密码包含特殊字符（如 @、#、$、% 等），需要用引号包裹
# 2. 示例: MYSQL_PASSWORD="my@password#123"
# 3. 如果密码包含引号，使用转义: MYSQL_PASSWORD="my\"password"
# 4. 简单密码可以直接写: MYSQL_PASSWORD=123456
MYSQL_PASSWORD=your_password
MYSQL_DB=your_database
MYSQL_CHARSET=utf8mb4
MYSQL_POOL_SIZE=5
MYSQL_POOL_MAX_OVERFLOW=5

# MongoDB 配置（可选，优先级高于 MySQL）
# 格式: mongodb://[username:password@]host:port
# 无密码: mongodb://localhost:27017
# 有密码: mongodb://username:password@localhost:27017
# 集群: mongodb://host1:27017,host2:27017,host3:27017
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=scra
MONGODB_COLLECTION=articles

# Scrapy 配置
CONFIG_PATH=demo.json
LOG_LEVEL=INFO

# 队列配置
SCRAPY_START_KEY=fetch_spider:start_urls
SUCCESS_QUEUE_KEY=fetch_spider:success
SUCCESS_ITEM_KEY=fetch_spider:data_items
SUCCESS_ERROR_KEY=fetch_spider:errors

# requests_worker 配置（可选）
REQUESTS_TIMEOUT=30
REQUESTS_MAX_RETRIES=3
REQUESTS_RETRY_DELAY=1.0
REQUESTS_SLEEP=1.0

# 测试接口服务配置
TEST_API_PORT=5001
TEST_API_HOST=0.0.0.0
TEST_API_DEBUG=true

# 代理配置（可选）
# 代理模式：static（固定代理）或 dynamic（动态代理）
# - static: 使用下面配置的固定代理地址
# - dynamic: 通过接口获取动态代理
PROXY_MODE=static

# 固定代理配置（当 PROXY_MODE=static 时使用）
# HTTP 代理配置说明：
# 1. 格式: http://host:port 或 http://username:password@host:port
# 2. 示例: HTTP_PROXY=http://127.0.0.1:7890
# 3. 如果不使用代理，留空或注释掉即可
HTTP_PROXY=
HTTPS_PROXY=
# SOCKS5 代理格式: socks5://host:port 或 socks5://username:password@host:port
SOCKS_PROXY=

# 动态代理配置（当 PROXY_MODE=dynamic 时使用）
# 动态代理API接口地址，返回格式应为 JSON: {"proxy": "http://host:port"} 或 {"http": "http://...", "https": "https://..."}
DYNAMIC_PROXY_API=http://your-proxy-api.com/get
# 动态代理API请求方法（GET/POST）
DYNAMIC_PROXY_API_METHOD=GET
# 动态代理API请求头（JSON格式，可选）
DYNAMIC_PROXY_API_HEADERS=
# 动态代理刷新间隔（秒），0表示每次请求都获取新代理
DYNAMIC_PROXY_REFRESH_INTERVAL=0
